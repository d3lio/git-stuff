[alias]
    graph = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
    ls = diff-tree --no-commit-id --name-only -r

	st = status
	co = checkout
	up = push origin
	down = pull origin
	amend = "commit --amend"

	new = "!node ~/git/scripts/new.js"
	ff = "!node ~/git/scripts/ff.js"
	fixup = "!node ~/git/scripts/fixup.js"
[core]
	filemode = false
[user]
	# name = Delyan Dobrev
	# email = delqn.dobrev@gmail.com
